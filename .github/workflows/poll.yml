name: Poll Action

run-name: Scraping oinp page and sending results

on:
    schedule:
      - cron: "27 */4 * * *"
    workflow_dispatch:
      inputs:
        textBasedTest:
          description: "Use the provided testing text file snapshot?"
          required: true
          default: false
          type: boolean


permissions:
  contents: write

jobs:
    poller:
        runs-on: ubuntu-latest
        steps:
            - name: Trying to install python 🐍
              uses: actions/setup-python@v3
            - name: Checkout main branch code 🧬
              uses: actions/checkout@v4
              with:
                ref: main
            - name: Setting Up Environment 🖥️
              run: pip install -r requirements.txt
            - name: Running the scraper 🛠️
              run: python ./scraper.py
              env:
                TWILIO_ACCOUNT_SID: ${{secrets.TWILIO_ACCOUNT_SID}}
                TWILIO_AUTH_TOKEN: ${{secrets.TWILIO_AUTH_TOKEN}}
                KUSHAGRA_NUMBER: ${{secrets.KUSHAGRA_NUMBER}}
                MAHAK_NUMBER: ${{secrets.MAHAK_NUMBER}}
                CHIRAG_NUMBER: ${{secrets.CHIRAG_NUMBER}}
                FROM_NUMBER: ${{secrets.FROM_NUMBER}}
                TEXT_TEST: ${{inputs.textBasedTest}}